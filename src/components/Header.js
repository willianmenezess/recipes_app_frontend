import React from 'react';
import SearchBar from './SearchBar';

function Header() {
  return (
    <section>
      <div>Header</div>
      <SearchBar />
    </section>
  );
}

export default Header;
